MAS gambler {

	infrastructure: Centralised // no distribution, the whole MAS runs in the same host

	environment: env.BlackjackEnvironment(1000, 1) // (fresca, debug)

	agents:
		gamblerhilo gamblerhilo ;
		dealer dealer ;

	aslSourcePath:
		"src/main/asl";
}
